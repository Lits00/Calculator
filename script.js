let num1, operator, num2;

function add(a, b) {
    // console.log(a + b);
    return (a + b);
}

function subtract(a, b) {
    // console.log(a - b);
    return (a - b);
}

function multiply(a, b) {
    // console.log(a * b);
    return (a * b);
}

function divide(a, b) {
    // console.log(a / b);
    return (a / b);
}